Quickstart
==========

`sis.SpotTable` is the backbone of the SIS package. It holds and aligns transcript and image information from a spatial transcriptomics experiment.

.. function:: enumerate(sequence[, start=0])

   Return an iterator that yields tuples of an index and an item of the
   *sequence*. (And so on.)